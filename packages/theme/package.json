{
  "name": "@mcplease/theme",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The design tokens + the PandaCSS preset — the single TS source of visual truth. The agnostic core (tokens/accents/motion) emits structured values only; preset.ts is the web binding. og-card.ts is the pure share-meta builder the edge functions read. Token values are placeholders until the design system pass lands.",
  "sideEffects": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./preset": "./src/preset.ts",
    "./og-card": "./src/og-card.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage"
  },
  "devDependencies": {
    "@pandacss/dev": "1.11.3",
    "@vitest/coverage-v8": "3.2.6",
    "typescript": "5.9.3",
    "vitest": "3.2.6"
  }
}
