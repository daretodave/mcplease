{
  "name": "@mcplease/slug",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The friendly URL slug rules — the single source of truth for what a valid slug is: charset, length, no edge/double dash, the reserved set. Mirrored by the DB unique index + create trigger; the realtime check is advisory, the index is the arbiter.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "3.2.6",
    "typescript": "5.9.3",
    "vitest": "3.2.6"
  }
}
