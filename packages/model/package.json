{
  "name": "@mcplease/model",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The shapes both sides share: the generated Supabase DB types plus the pure domain types (the link kinds, the public projection). Names no vendor — it's just shapes.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage",
    "gen:types": "supabase gen types typescript --local > src/database.types.ts"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "3.2.6",
    "typescript": "5.9.3",
    "vitest": "3.2.6"
  }
}
