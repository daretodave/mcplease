{
  "name": "@mcplease/analytics",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The mcplease tracking law as TypeScript: the typed GA4 event taxonomy. Every event is snake_case, surface-prefixed (create_/share_/edit_/system_), carries only low-cardinality enums — never a server URL, never PII. `track.*` is the only sanctioned push path; the /web binding is the GA4 sink.",
  "sideEffects": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./web": "./src/web.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "3.2.6",
    "typescript": "5.9.3",
    "vitest": "3.2.6"
  }
}
